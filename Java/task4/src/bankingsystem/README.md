# Task4 : Simple Banking System

A simple banking system that allows users to create accounts, delete accounts, deposit money, withdraw money, check balance, and transfer money between accounts.

## Usage

- Compile: in `src` directory
    ```bash
    javac bankingsystem/*.java
    ```
- Run: in `src` directory
    ```bash
    java bankingsystem.ConsoleApp
    ```

